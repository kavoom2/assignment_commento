import initialState from "./initialState";

const filterCheckedReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default filterCheckedReducer;
